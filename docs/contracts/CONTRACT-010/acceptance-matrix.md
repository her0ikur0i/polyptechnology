# First-release acceptance matrix

Date: 2026-08-08

| #   | Requirement                                                                          | Status   | Direct evidence                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Authenticated owner creates a conversation and reviewed contract                     | Verified | `OwnerCommandService` rejects unauthenticated/CSRF-invalid calls, creates a project-scoped conversation and idempotent `owner_review` proposal; PostgreSQL integration and dashboard command tests pass.                                                                                                                                         |
| 2   | Contract survives API/worker/host restart without duplicate work                     | Verified | PostgreSQL conversation/supervisor reconstruction, work-engine reconstruction, operation evidence idempotency, fenced leases, and reconstructed supervisor tests pass.                                                                                                                                                                           |
| 3   | Isolated worker delegates bounded coding and stores evidence                         | Verified | Digest-pinned network-denied worker-plan/executor tests and immutable operation evidence pass; managed DeepSeek worker attempts are attributed and verified.                                                                                                                                                                                     |
| 4   | Deterministic verification rejects intentionally incorrect work                      | Verified | CONTRACT-010 integration submits a deliberately wrong expected digest; the task becomes `failed` and cannot publish success. Existing artifact and publication verifiers also fail closed.                                                                                                                                                       |
| 5   | Successful contract creates one scoped commit and push                               | Verified | CONTRACT-001 through CONTRACT-009 each have one bounded commit on `origin/main`; publication path/gate/SHA recovery tests pass. CONTRACT-010 publication is performed only after its final gate.                                                                                                                                                 |
| 6   | Cost attribution reaches provider -> agent -> project -> contract -> task -> attempt | Verified | Immutable gateway usage rows carry provider/model and complete attribution; PostgreSQL gateway tests and per-contract evidence reconcile concrete token/cache/cost totals.                                                                                                                                                                       |
| 7   | Factory Live displays real events and pauses when hidden                             | Verified | Runtime-validated snapshot/SSE projection, gap recovery, stale/replay state, visibility/intersection/reduced-motion pause, semantic hierarchy, renderer tests, and production build pass.                                                                                                                                                        |
| 8   | Dangerous action fails without scoped approval                                       | Verified | Telegram approval token/identity/expiry/single-use tests, proposal handoff gate, generated-project risk-state approval gate, worker capabilities, and owner command authentication/CSRF tests pass.                                                                                                                                              |
| 9   | Backup restore rebuilds durable state in a clean environment                         | Verified | Custom-format PostgreSQL dump SHA `2792dcc37dcdaa0b8c9ae30c7f6da86cd627287576fb9947df1b8194293213be` (135,407 bytes) restored into a clean database; both sides contained 48 public tables and restored application tests passed 3/3. Durable manifest/restore evidence SHA: `2812b42591c64684458792ef55c631e6cc77edfc010bd11aacf3d32ca0349160`. |
| 10  | No anonymous public mutation endpoint exists                                         | Verified | Production config fails closed without access authentication; owner commands independently require authenticated owner context and timing-safe CSRF match. Service artifacts bind behind the approved access boundary; no direct public deployment exists.                                                                                       |
| 11  | Emergency stop prevents new work and drains/cancels safely                           | Verified | Work-engine PostgreSQL tests prove active lease cancellation and new-lease denial. CONTRACT-010 additionally proves an in-flight signal-aware driver aborts on heartbeat failure after emergency stop and returns to a resumable queued state.                                                                                                   |
| 12  | One dummy generated project completes blueprint -> demo                              | Verified | Fresh PostgreSQL acceptance creates an arbitrary versioned blueprint and dynamic isolated project, then persists four legal, evidenced, fenced transitions through `demo` without external provisioning.                                                                                                                                         |

All statuses above are supported by current repository/runtime evidence. Production
activation and external cutover are operational authority decisions, not substitutes
for these capability tests, and are listed separately in the Owner Action Bundle.
